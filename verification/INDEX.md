# Claim → verification index

Every load-bearing claim in the two papers, mapped to how it is established: an **analytic proof**
in the paper, a **computational verification** script here, or both. Scripts are self-contained
(Python 3 + numpy/sympy); run from this directory. Each entry lists the one-line result to expect.

## Paper A — *The Canonical Class of the MASA Context Stack and the Peres–Mermin Obstruction*

| Claim | Statement | How established |
|---|---|---|
| Thm A | $\mathrm{U}(1)$ lifting class is vacuous on $\mathrm{U}(n)\ltimes X$ | analytic (paper §1) |
| Prop (Morita) | reduction to $\mathrm{PU}(n)\ltimes X$ | analytic (paper §2) |
| **Thm G** | canonical class has **order exactly $n$** | analytic (equivariant-hom lemma) + `thmG_general.py` (dimension-independent stress test) → `thmG_general PASS` |
| Thm B | real shadow splits **iff $n$ odd** ($-I$ is a rotation square iff $n$ even) | analytic (paper §3) |
| Thm E / F | $n=4$ Pauli slice: PM pairing $=-1$, equals the obstruction | analytic (paper §4–5) |
| Prop H2 | $H^2(\mathrm{PN}(T);\mathrm{U}(1))=\mathbb{Z}/n\oplus\mathbb{Z}_2$, canonical class generates $\mathbb{Z}/n$ | analytic (paper §7, spectral sequence) |
| **Even-n generation (NEW 2026-07-12)** | canonical class generates the $\mathbb{Z}/n$ summand for **every $n$ incl. $n=4$** (permutation splitting $\Rightarrow$ zero Schur component; + order $n$) | `paperA_evenN.py` (`paperA_evenN PASS`) + `papers/paperA_foundations.tex` §resolution |
| **ACC correspondence (NEW 2026-07-12)** | Paper B Thm 1 = ACC (FSCD 2024) Thm 16 in the Weyl realization: their Lemma-15 polarization identity holds on Weyl labels; certificate values $S$ = their central phase $k=d/2$ (PM, cert8, cert16) | `acc_correspondence.py` (`ACC-correspondence PASS`) + `papers/paperB_ACC_comparison.tex` |

## Paper B — *The Obstruction Spectrum of Weyl Context Families and the 2-adic Tower*

| Claim | Statement | How established |
|---|---|---|
| **Thm J** | Obstruction Spectrum: $2S\equiv0\ (\mathrm{mod}\ d)$; achievable value set $\{0,d/2\}$ (even $d$), $\{0\}$ (odd). **Classification due to Abramsky--Cercelescu--Constantin, FSCD 2024** (their Thm.~16/21); recovered here in Weyl-certificate language | analytic + `spectrum_test2.py` (odd-$d$ ⇒ only 0) → `spectrum_test2 PASS`. **Its section B-strong is a labelled EXPECTED NEGATIVE** (the mod-$2d$ refinement is false, ~860–1020 violations per 2000; not asserted) |
| Thm J′ | commutator-carry form of the criterion | analytic + folded into `criterion.py` |
| Thm K | Depth Rigidity: obstruction lives at top 2-adic layer, no single lift carries it | analytic (paper §4) |
| **Thm W** | exact value-bit formula for lifted certificates | `Wformula.py` → `violations 0/550` (×4 families), verdict `Wformula PASS` |
| **Thm Q** | closed-form criterion: contextual ⟺ some cycle has odd $Q=\sum_{a<b}\langle v_a,v_b\rangle/d$ (Pontryagin-square-type quadratic refinement) | `criterion.py` → `criterion PASS` (incl. an in-script recomputed random-cycle control; the printed "300/300" is a quoted historical figure) |
| **Thm attain** | attainment at every even $d$; explicit certificates | `verify_cert8.py` → `ALL CHECKS PASS` ($S{=}4$); `verify_cert16.py` → `PASS` ($S{=}8$) |
| **Prop. 9 CORRECTED (V55, 2026-07-28)** | the odd-$Q$ shadow has **NO false positives at any even $d$** — proof: $Mx\equiv\gamma \bmod d$ with $d$ even gives $Mx\equiv\gamma \bmod 2$, so $\lambda^{\mathsf T}M\equiv0 \bmod 2 \Rightarrow \lambda\cdot\gamma\equiv0 \bmod 2$. It **is** incomplete, in the OTHER direction: a $\Z_d$-cycle can certify with a nonzero but EVEN value, invisible mod 2 | `shadow_soundness_exact.py` → `shadow_soundness_exact PASS` (0 false positives over 571+ firing samples; pinned non-degenerate $d=8$ blind witness; ~39% miss rate under the stated protocol) |
| Doubling law (geometric step) | $T_{\mathrm{mix}}\equiv0$; $\mathbb{F}_2$ system is $d$-independent | `close_T2_proof.py` (structural, **proof CORRECTED 2026-07-24** — the old per-context route (P1) is false in bulk; the identity holds via the quadratic refinement + the realized base-multiplicity sublattice $N$) + `tmix_dindep.py` (byte-identical across $d\equiv2\bmod4$) → `tmix_dindep PASS` |

### Correction to Paper B, Prop. 9 (2026-07-28, published as Paper B v2)

Paper B v1 stated that the odd-Q shadow can produce a **false certificate**, quoted 28.6% as the
rate among random d=8 incidence systems carrying a Z_2-odd cycle, and cited an explicit d=8
example in this repository. All three are wrong.

For even d, `Mx = gamma (mod d)` solvable implies `Mx = gamma (mod 2)`, so any lambda with
`lambda.M = 0 (mod 2)` has `lambda.gamma = (lambda.M).x = 0 (mod 2)`. The shadow cannot fire on a
noncontextual system: **the false-positive rate is 0 at every even d**, and no such certificate
exists. The criterion is incomplete in the opposite direction — a Z_d-cycle can certify with a
nonzero but even value, invisible mod 2. Certificate and pinned witness:
`shadow_soundness_exact.py`.

What stands from v1 is the point about certificate minimization: a minimal odd-Q *support* need
not be a Z_d-cycle, so it can be a false *witness* while the *verdict* is always correct.

## Paper C — *What Contextual Holonomy Detects*

Every token below is what the current code prints; all were re-run 2026-07-27.

| Claim (paper C) | Statement | Script → expected one-line output |
|---|---|---|
| Thm 1 (Equivalence, AvN sector; **even $d$**) | unsolvability of $A\gamma\equiv s$ $\iff$ some cycle has odd self-pairing $Q$ | `holonomy_vs_solvability.py` → `d=2: 1500 random families, unsolvable 3, equivalence mismatches: 0` / `d=4: 600 random families, unsolvable 0, equivalence mismatches: 0` / `pinned cert4 d=4: unsolvable=True, odd-cycle=True, agree=True`. Note the $d=4$ random draw contains **zero** positive instances, so that direction is exercised at $d=4$ only by the pinned certificate. |
| Prop 1 (nontrivial class, zero AvN) | order exactly 3 at $d=3$, achievable AvN value $\{0\}$ | `d3_gap_certificate.py` → `GAP CERTIFIED: nontrivial order-3 class AND AvN value {0}` (0 coboundary hits over all $3^9=19{,}683$ cochains) |
| Prop 2 (contextuality, zero class shadow) | KCBS $\sqrt5$ vs classical 2, class shadow 0 | `kcbs_converse.py` → `PASS: contextual (2.23607 > 2) with zero d=3 class shadow => converse gap certified` |
| Obs 1 (Wigner–friend level split) | projective holonomy $+1$, determinant-section holonomy $-i$ | `wf_loop_holonomy.py` → `ray-level: strands +0.25pi, -0.25pi; loop product = 1, gauge-invariant 200/200: True` / `det-section holonomy over 500 SU(2) closers: {-1j}  (square = -1)` / `PASS` |
| Lemma 1 (net necessity) | — | `ghw_net_necessity.py` → `PASS` (`Lagrangians: 15, rank over F2: 10`) |
| 1024-net sweep | — | `gf4_net_necessity.py` → `PASS` |
| $2^{15}$ frame sweep | net-robust-negativity classifier refuted on this family | `net_robust_negativity.py` → `PASS` (`11 CF=0 robustly-negative states`) |
| Thm 2 constructive half | — | `paired_frame_construction.py` → `PASS(unified)` |
| ghost facet theorem + closed CF formula | 24 = 20 in-context + 4 ghost | `ghost_facet_theorem.py` → `PASS(completion)` |
| gauge invariance of the PM witness | 512 gauges | `pm_gauge_invariance.py` → `six-sign product over all 512 sign gauges: {np.float64(-1.0)}` / `PASS` |
| $d=4$ tier-2 arithmetic | 61 classes, 18 species, **all functionals rational** | `d4_arithmetic_profile.py 2` → `All tier-2 facet functionals are RATIONAL: they admit exact {0,+-1}-coefficient lifts in the 150 character coordinates with integer bounds {6,7}` — **the certificate behind Retraction 1 of paper C** |

**Every row above now pins an actual output string**, re-run 2026-07-27. Paper C's promise that
"expected one-line outputs are pinned in `verification/INDEX.md`" is, for the first time, true.

**Scope limits stated in Paper C itself**, and repeated here because they bear on how the
entries above should be read:

- Theorem 1 is asserted for **closed-triple** Weyl families at **even** d. Neither hypothesis is
  decorative; the paper says so.
- Theorem 2 is **machine-validated (40/40), not proved**, in the generality in which it is stated.
  The closed form is proved end to end for the deletion families treated. The general
  compressed-coordinate statement is open — see V52(ii).
- Two sweeps quoted in the paper contain **no positive instances**: the d=4 half of the
  2,100-family equivalence test, and the 6,500-trial false-certificate sweep in `shadow_gap.py`,
  which draws no contextual family at any of d=2,4,8. They are consistency checks on the
  implementation, not searches that could have failed. Both are disclosed in the paper.

## Open (honestly labelled in the papers)
- Paper B: base-level closed form for **generativity** of the doubling law (governed by the standard
  odd-carry-cycle criterion; no slicker base-level invariant found).
- Paper A: **Yu–Oh** monomial-shadow scope; Dixmier–Douady gerbe refinement.

## Hardware
See `../hardware/` — 3-device $d=2$ Peres–Mermin replication, combined $S=4.76$ at **$80\sigma$ relative to shot-noise statistical uncertainty under the implemented witness model** (NOT a loophole-free certification; sequential-measurement compatibility/nondisturbance controls and systematic uncertainty are assumptions; the devices FAIL a homogeneity test (χ²≈266/2), so the systematics-aware combined significance is ≈7σ, NOT 80σ — see `../hardware/COMPATIBILITY.md`);
`verify_combined.py` recomputes it from the raw per-context correlators.
- V23 `local_classicality.py`: every single context's restricted Weyl cocycle is a coboundary; verified d=2 (PM), d=3, d=4 (cert4); global obstruction contrast via V10/V13/V33. **SS2 claim closed.** **CORRECTED — see V46 (`mu_d_valuation.py`) for the SHARP valuation: trivializer valued in mu_d for even d (no tau-steps) and mu_{2d} for odd d (sharp at d=3). The earlier "mu_{4d} / half-tau-steps genuinely needed at even d" wording is RETRACTED (layout artifact; see the V46 entry and the 2026-07-11 correction ledger below).**
- V36 `d4_tier2_catalogue.py` **CORRECTION**: completeness of the tier-2 facet family RETRACTED after fresh-sample audit; exact facet arithmetic (rank 32, |c|=3/(2 sqrt 2), bounds {6,7}) stands; automorphism-orbit completion open.
- V37 `lueders_instrument.py`: instrument-level Lueders characterization — statistics + repeatability + commutant-covariance pin the LINEAR solution to a one-parameter **affine depolarizing line** per degenerate block; complete positivity then gives the exact interval **−1/(d_j²−1) ≤ p_j ≤ 1** (NOT [0,1]); efficiency (single Kraus) ⟺ p_j=1 = Lueders (unique single-Kraus member); MASA-only covariance is strictly insufficient. **EVIDENCE LABEL: numerical affine-dimension stress test** (Haar sampling + float lstsq + numerical rank; CP not imposed here) — verified at (4,[2,2]) and (6,[3,2,1]); the CP interval is certified exactly in `lueders_cp_interval.py`. **SS4 instrument gap addressed.** *(CORRECTED 2026-07-11: was mislabeled "machine-exact" with range [0,1]; see ledger below.)*
- V37b `lueders_cp_interval.py`: **exact rational-arithmetic Choi-eigenvalue certificate** (Python `Fraction`) of the CP interval −1/(r²−1) ≤ p ≤ 1 for r=2..5 (endpoints and just-outside rejections); rank-1 collapses to the identity. Expected: `V37b PASS`.
- V38 `observer_groupoid.py`: the two Paper D certificates — (A) pullback monotonicity: PM (x) I_F at d=8 keeps products (+1 x5, -1 x1), the obstruction survives Wigner enlargement; (B) hol^2 in {+-1} is a gauge-robust Z2 invariant over Weyl regaugings and SU closers: -1 for the MUB triangle, +1 for within-context loops. **Copenhagen row: Paper D drafted (papers/paperD.tex), pending adversarial review.**
- Paper D adversarial self-pass #1 (v0.2): FOUND & FIXED — (1) Thm 2 headline overclaim: hol^2=-1 is NOT equivalent to complementarity (pinned counterexample: complementary 2-cycle (H,H) gives +1); correct claim is necessity + the oriented-MUB-triangle detection, invariant = tau-switch parity. (2) "every stabilizing loop" was false for non-Weyl stabilizers (diagonal dets arbitrary); scope restricted to Weyl switches. (3) hol^2 invariance upgraded from sampled to PROVED (all Weyl dets +-1, machine-checked). (4) morphisms between different cuts were ill-composed; groupoid now generated by switches (fixed enlargement) + refinement isometries. (5) Thm 1(b)/Thm 3 reworded to family-level value assignments / section-fixed comparison maps.
- V39 `fr_cycle.py`: the Frauchiger-Renner observer cycle LOCALIZED — (i) trivial class (phases all +1, |L|=16) and switch-loop hol = +1 exactly, fully gauge-invariant (all 2-qubit Weyl dets +1): FR is NOT a holonomy paradox, refuting Paper D's own conjecture; (ii) state-sector: CF = 1/6 exactly, codeword-polytope exit certified, unique separating facet = CHSH at quantum value 7/3, ABM normalization matches the LP to 1e-6. **Paper D Theorem 4 (v0.3).**
- V40 `s4_povm_bayes.py`: the S4 treatment's certificates — (A) SHARP-CORE THEOREM: (i)+(ii) force outputs into the mutually orthogonal 1-eigenspaces of the effects **for every outcome**; strictly unsharp effects admit NO repeatable instrument; "repeatable ⇒ projective" is FALSE but a witness must be **≥3-dimensional** (every nonzero outcome needs an eigenvalue-1 sharp core). Valid witness: qutrit E1=diag(1,0,c), E2=diag(0,1,1−c). On the core the family is V37's affine line. (B) CLASSICAL TWIN: same three axioms give the affine family **T_t = t·I + (1−t)·J/m per cell, −1/(m−1) ≤ t ≤ 1**; deterministic covariant members are the identity (t=1, "Bayes") for all m AND the swap (t=−1) for m=2, so Bayes is the unique deterministic member only for m≥3; nullity 2 on [2,2], 1 on [3,1]. **S4 closed.** **CORRECTION 2026-07-11: the earlier pinned qubit counterexample diag(1,c) was INVALID — its outcome 2 (E2=diag(0,1−c)) has no eigenvalue 1 and is not repeatable; the old script checked repeatability only for outcome 1. Replaced by the qutrit example (both outcomes checked; old example kept as a pinned negative regression). The classical range was also corrected from [0,1] to [−1/(m−1),1] with the m=2 swap noted. `note_v2_patch.md §S5` predates this fix.**
- V41 `d4_tier2_orbit.py`: the tier-2 AUTOMORPHISM GROUP DERIVED — the family is K3,3 (contexts=nodes, observables=edges); automorphisms fixing the deleted context = 12 graph autos x 64-torsor of spectral shifts x conjugation (order 1536); every generator provably BIJECTS the 64 vertices; exact monomial moment-space action (no snapping). Two caught bugs pinned: source-indexed shifts; mixed-grid keys (facet coefficients live on 1/4 Z UNION 1/(2sqrt2) Z — incommensurable grids). Orbit of the original 9 seeds: 12288 exactly valid facets.
- Census status (honest): 14 seed classes found to date, all the SAME arithmetic species (rank 32, |c|=3/(2sqrt2) uniform, bounds {6,7}); extended orbit 21504 facets (d4_tier2_orbit_data.npz), fresh-sample coverage 159/160 and rising per class — sampling converges but cannot certify completeness.
- V42 `d4_exact_dd.py` (PREPARED, PENDING COMPUTE): exact settlement — the polytope is just 64 integer {-1,0,1} points in faithful dim-33 integer coordinates; its H-representation is the complete catalogue as a theorem. DD exceeded the interactive budget; run overnight (Colab): `python3 d4_exact_dd.py`.
- V42 progress: lrs measured at ~1 facet/sec (1392 exact integer facets streamed and verified: d4_exact_dd_partial.npz); QUANTUM MOMENT VECTORS PROVEN ON-HULL numerically (residual ~1e-15) => the finished H-rep alone is the complete classifier. Ridge-walk-modulo-group designed as the in-session completeness route.
- `d4_facet_census.py` (V43): **COMPLETE FACET CENSUS + CLASSIFICATION THEOREM, d=4 DROP=0** - conv(V) has exactly **23,256 facets in 61 classes** (|G|=768); quantum moments on-hull => **CF(rho)>0 <=> a facet is violated**. TWO independent derivations agree: (1) adjacency decomposition (facet-ridge BFS modulo G, every pivot exactly certified via integer nullspace recovery; heavy facets' ridges via cdd/Normaliz after lrs wedged reproducibly at 3195 rows - pinned); (2) Normaliz 3.10.2 direct enumeration (old input format; 23,256 exact hyperplanes). Method lesson pinned: an in-walk "121 classes / 46,508 facets" was a class-dict KEY-TYPE duplication (int vs bytes keys double-storing each orbit) caught ONLY by the independent cross-check. Data: `d4_facet_classes.npz` (61-class table + full 23,256 exact integer facets + pivot columns).

## V-number conventions (read before citing a V-number)

The authority for what a V-number means is **this file plus the header of the named script**.
`ROADMAP_30.md` is a planning document: several slots there were never built and their numbers
were later reused by shipped work. Where the two disagree, this file wins. The known
divergences, stated rather than silently patched:

| V | THIS FILE / script header (authoritative) | `ROADMAP_30.md` (planned, not built) |
|---|---|---|
| V10 | `local_classicality.py` — per-context trivializers, and the global contrast | `pm_gauge_invariance.py` |
| V13 | `d3_gap_certificate.py` — order-3 Heisenberg class nontrivial, qutrit AvN value {0} | same |
| V28 | `net_robust_negativity.py` — net-robust negativity; sufficiency refuted | `wf_interferometry_spec.py` |
| V29 | `ghw_net_necessity.py` — necessity on genuine GHW nets | `convention_fuzz.py` |
| V30 | `gf4_net_necessity.py` — necessity on the actual GF(4) Gibbons–Hoffman–Wootters nets | `property_harness.py` |
| V31 | `paired_frame_construction.py` — the constructive half; codeword-polytope theorem | — |
| V32 | `ghost_facet_theorem.py` — ghost facet theorem and closed formula | — |
| V33 | `d4_ghost_tower.py` — tower ghost law, offset d/2 at d=4 | — |

`V54` was used twice in earlier revisions of this file. It now means **`pm_local_system.py`**
(the Peres–Mermin two-layer milestone) — that is the sense the Copenhagen note cites. The Paper B
Prop. 9 correction, which nothing outside this file cites, is renumbered **V55**.

- `repeatable_efficiency_dichotomy.py` (V56): **THE REPEATABILITY/EFFICIENCY DICHOTOMY.** (a) An EFFICIENT (single-Kraus) repeatable instrument forces its effect to be a projection, in every finite dimension — proved (statistics gives K^dag K = E; repeatability gives K^dag(I−E)K = 0 hence ran K ⊆ Eig_1(E); polar decomposition gives rank E ≤ dim Eig_1(E), and the reverse always holds). Stress-tested: 4000 non-projective effects × 20 unitaries each, 0 efficient repeatable instruments found. (b) WITHOUT efficiency the folk claim fails at d=3 and d=3 is minimal: E_1=diag(1,0,c), E_2=diag(0,1,1−c) is repeatable on both outcomes, covariant under the commutant (worst residual 2e-16), E_1 non-projective, needs 2 Kraus operators. **PRIOR ART, cited in the note and not claimed as new:** the sharp-core/norm-1 half is Ozawa (JMP 25, 79, 1984), Busch–Grabowski–Lahti (Found. Phys. 25, 1239, 1995), Heinonen–Lahti–Pellonpää–Pulmannová–Ylinen (JMP 44, 1998, 2003), BLPY *Quantum Measurement* (Springer 2016); (a) is the finite-d content of Buscemi–D'Ariano–Perinotti (PRL 92, 070403, 2004), whose "nonorthogonal repeatability occurs only for infinite dimensions" is stated for PURE measurements — one contraction per outcome, i.e. exactly efficiency. That is why (a) and (b) are consistent with BDP rather than contradicting them. New here: the dichotomy stated as such, the covariance of the witness, the d=3 minimality.
- `d4_ghost_tower.py` — the d=4 ghost-rotation tower laws cited in the note's claims-to-verification table (ghost rotation d/2; stratification of operative facets by operator order). Companion to `d4_moment_facets.py`.
- V43 addendum: **ON-HULL LEMMA proven** (exact integer rank): quantum behaviors satisfy normalization + shared-marginal + 24 spectral-support identities; constrained affine dim = 33 = deterministic span. The census classifier theorem is now unconditional. Incorporated in `d4_facet_census.py` and stated as Prop. in note v3.
- `papers/note_v3_local_validity.tex` — the Copenhagen synthesis note: the five tenets as a theorem schedule, every claim citing papers A–D or a script in this suite, conjectures and open problems labeled as such. The hardware figures are shot-noise only under the implemented witness model (see `hardware/COMPATIBILITY.md`).
- `solvability_equivalence.py` (V44): **DETECTION EQUIVALENCE PROVED** — for closed-triple Weyl families at even d: no NC assignment ⟺ some kernel vector pairs to d/2 ⟺ odd commutator carry. Assembly: constructive SNF double-annihilator over Z_d + Paper B Thm 1 (spectrum, any multiplicity, Rem. 3) + Thm 4. Includes a **gauge/integrality clarification**: the GF(2) system is always solvable (a Thm-1 consequence) and kernel pairings are gauge-invariant. By V46, every commuting context at even d has even τ-exponent, so context products are automatically ω-integral and no gauge-integrality hypothesis is required; the earlier "PM is τ-odd / integrality is a gauge choice" remark was a coordinate-layout artifact and is retracted (see the 2026-07-10 CORRECTION+RETRACTION entry below). Certified (numbers CORRECTED 2026-07-10, see the CORRECTION+RETRACTION entry below; this line previously carried the pre-correction counts 461/27/16): **462** genuinely commuting gauged families at d=2 (**350** unsolvable, all witnessed), **9** at d=4 (2 unsolvable) + pinned cert4; spectrum confinement and double-annihilator exact throughout. Upgrades note v3 Thm 3 from criterion+verified-converse to full equivalence.
- **Adversarial review of note v3 + paper D (2026-07-10)**: author's external pass — no flaws found. An independent cross-check of the named attack surfaces (the cross-check is not optional) found no invalid theorem but four precision items, fixed in-tex and pinned here: (F1) Thm 3 proof wrote the SNF witness as u=(d/g_i)U_i; the invariant factor g_i need not divide d — correct witness is u=(d/gcd(g_i,d))U_i (same u when g_i | d; theorem unaffected). (F2) The gauge lemma's justification "by the same spectrum theorem" was loose — replaced by the CONSTRUCTIVE proof via the q-parity identity (V45), which also delivers the τ-general statement of V44 (roadmap item closed). (F3) note v3 Thm 1 stated the μ_{4d} valuation of the trivializer unconditionally; the repo's evidence is instance-level (V23: d=2,3,4; V45: d=6,8,12) — theorem reworded to match paper D's honest scope; U(1)-coboundary remains general (Ext(G,U(1))=0). (F4) Principle (iii) said "covariance" where the theorem needs commutant-covariance (MASA-only is insufficient, V37) — wording fixed. Rem. 3 multiplicity scope CONFIRMED adequate: paper B defines certificate as any λ ∈ Z_d^ctx with λᵀA ≡ 0, exactly the kernel vectors Thm 3 uses. Score held at 99 pending the author's review of these fixes.
- **CORRECTION + RETRACTION (2026-07-10, found by continuing the cross-check)**: (1) **V44 layout mismatch, fixed**: `solvability_equivalence.py`'s pinned PM labels and its `sym()` used the block layout (a1..am,b1..bm) while `weyl.build`'s W reads per-site pairs (a1,b1,a2,b2,...); for m≥2 the "commuting" filter did not match the operators — 2/6 pinned PM triples were NON-commuting operator families (regression pinned in V46). Labels and form corrected; a per-context operator-commutation assert added; rerun PASS (462 genuinely commuting families at d=2, 350 unsolvable — the true PM square now included; 9 at d=4). The combinatorial equivalence itself held on both the polluted and the corrected samples. (2) **"PM is τ-odd" gotcha RETRACTED as artifact** of (1): by the parity lemma (V46), at even d every commuting pair has EVEN c(u,v) (parity = ⟨u,v⟩ mod 2), so every commuting closed context has even τ-phase — context products are always ω-powers and **integrality is automatic, not a gauge choice**. The q-parity gauge (V45) remains true and is the correct statement for general (non-commuting) closed-triple data. (3) **"Half τ-steps necessary at even d" RETRACTED**; superseded by the sharp valuation below. State-sector τ-necessity (V33/V34, `d4_moment_facets.py`) is a different theorem, unaffected.
- `tower_holonomy.py` (V47): **TOWER CONJECTURE (note v3 Conj. 2) RESOLVED BY CASES** — (i) operator level: every closed Weyl word ∈ μ_{2d} (PROVED, telescope), exhaustive (PROVED even d via the pinned XZ-words with exponents e, e+2, e odd; verified odd d). (ii) det-section level, canonical τ-grid section: bound ∈ μ_{2d} PROVED for even d (all generator determinants computed exactly: det S = τ^{σ(d)}, σ(d)=d(d−1)(2d−1)/6; det F ∈ μ_4); **exhaustiveness REFUTED**: attained group = μ_{2d} iff d ≡ 2 (mod 4); along the 2-adic tower d=2,4,8,16 it is CONSTANT μ_4 — the base-case coincidence μ_4 = μ_{2d} at d=2 does not propagate. (iii) odd d is SECTION-DEPENDENT: DFT section violates the bound at d ≡ 3 (mod 4) (pinned: d=3 loop holonomy e^{iπ/6}, d=7 gives i; closer-independent 50/50), Weil normalization F_W = (√d/g)F restores it (verified d=3,5,7; general proof open). Scope caveat pinned: unrestricted sections make the statement vacuous — claims must pin the section (v1 lesson recurs). Expect: `c2_tower_probe done.` (<15 s).
- `scenario_paired_evidence.py` (V48): **CONJECTURE 1 EVIDENCE HARNESS, d=4 AND d=8** — CF(F,ρ)=0 ⟺ c(ρ) ∈ conv L(F) (scenario-paired = per-context character vector; L = spectral global sections): **5,556 records, 117 scenario instances, 0 mismatches** — all six deletions of the certified d=4 family (census-arbitrated: 578/578, corr(CF,−margin)=0.995), 15 double deletions, boundary bisections to 1e−6, 69 random families, 10 fresh anomalous magic squares (incl. genuinely odd order-8 phases) exercising the AvN branch non-vacuously; 51 distinct optimal facets and 1,398 violated facets hit (tier-2 √2 sector included). Records ship in `c1_records/` (~580 KB), regenerable via CLI modes; default no-arg run = selftest. Pinned side findings: PM doubled into d=4 is NOT AvN under the τ convention (|L|=16); doubling cert4 into d=8 kills the class (all s=0) — doubling descends the tower. PINNED GAP: paper C does not state the scenario-paired c(ρ) for general even d verbatim (fixed at d=2 and the census family only) — the definitional gap and the residual completion lemma (full character vector → paired coordinates, the general-family on-hull analogue) are the distance to a proof.
- `kcbs_holonomy_home.py` (V49): **KCBS COHOMOLOGICAL HOME — TWO NO-GOS + A WORKING DETECTOR (pentagon)** — (no-go 1) the KCBS-optimal empirical model has FULL support identical to a noncontextual model's and is not logically contextual, yet CF=0.472: every support-presheaf cohomology (AB Čech, higher obstructions, AvN classes) provably cannot characterize CF>0; (no-go 2) the same C5 exclusivity graph carries a never-contextual rank-1 realization — no invariant of the abstract scenario works; structural reason pinned: probabilistic contextuality is a positivity/cone failure, invisible to abelian coefficients. (positive) rank-1 realizations mod gauge = (Gram magnitudes, U(1) holonomy class on the non-orthogonality graph); on the pentagon: CF = 2·max(0,S−2) exactly (300 states, 1e−15); detector λ_max(twisted C5) with quantum maximum √5 = Lovász θ(C5) at holonomy +1; at FIXED magnitudes the holonomy class alone decides contextuality (g=0.52: φ=1.0 contextual, φ=1.5 not, both realized in d=5, LP-certified); contextual window exactly |φ|<π/2; the (−1)-twist of the KCBS point hits the classical bound 2 EXACTLY (ghost-tower echo, flagged as analogy). General formulation = paper C OP(a), still open. Expect: `C3 PROBE PASS` (<5 s).
- `kcbs_antihole_extension.py` (V50): **C7/C9/ANTIHOLE EXTENSION OF V49; TORSION SHADOW DEMYSTIFIED** — (i) V49's no-gos transfer verbatim to C7 (full support, not logically contextual, CF=0.635); facet uniqueness holds: CF = 2·max(0,S−α) to 1e−15 (C7: α=3; 240 states + 120 realizations, d=3..7); detector λ_max(twisted adjacency) > α characterizes contextual realizations, 0 mismatches, bounded by Lovász θ. (ii) All 8 fundamental holonomy coordinates of H¹(C̄7;U(1)) matter (detector Hessian negative definite, full rank 8); trivial class is detector-maximal at any magnitudes (Perron–Frobenius — small proven lemma); holonomy still decides at fixed magnitudes (LP-certified pair at non-uniform magnitudes; pinned departure: on the uniform slice no realizable noncontextual class was found). (iii) **V49's "(−1)-twist hits the classical bound exactly" is DEMYSTIFIED**: the full twist is the reflection G ↦ 2I−G, so twist value = 2−λ_min = 2 at ANY PSD-critical point of ANY scenario — it coincides with the classical bound iff α=2, i.e. the law lives in the α=2 ANTIHOLE family (verified constructively on C̄7: full pentagon phenomenology; C5 is the unique member of both families, resolving the "pentagon accident"). The ghost-tower analogy in the V49 entry should be read at this corrected scope. General two-torsion-layer formulation stated as flagged conjectures in the script header. Expect: `C7 PROBE PASS` (~2 s).
- `weil_section_theorem.py` (V51): **ODD-d WEIL-SECTION TOWER THEOREM (V47's open case CLOSED)** — for all odd d, every SU-closed loop holonomy over the full canonical generating set {W(v), F_W, S, M_a} lies in μ_{2d}. Proof by exact generator determinants: det X = det Z = 1; det W(v) = (−1)^{ab}; det S = ω^{binom(d+1,3)} ∈ μ_d (=1 iff 3∤d); det F = i^{d(d−1)/2} (Schur) — whence the **naive-DFT dichotomy is proven: bound violated iff d ≡ 3 (mod 4)**; det F_W = (−1)^{⌊d/4⌋} ∈ μ_2 (the Gauss phase exactly cancels the μ_4 part of Schur's phase in both residue classes); det M_a = Jacobi symbol (a|d) (Zolotarev/Frobenius). Since μ_2·μ_d = μ_{2d} at odd d, done. Corollary: Weil-section attained group = μ_6 if 3|d, else μ_2 — exhaustive iff d=3 (the odd-d analogue of V47's non-propagating base coincidence). Machine-verified d=3..15 (all d² Weyl reps, all units, 25 closers/loop, 300 random words/d). Expect: `cw_weil_probe PASS` (<30 s).
- `paired_classification_theorem.py` (V52): **PAIRED CLASSIFICATION: THEOREM + THE REAL RESIDUAL — scope is subtle, read carefully.** (i) In the FULL-CHARACTER reading pinned by V48 (c(ρ) = all per-context characters), Conjecture 1 is a **theorem**: CF=0 ⟺ c(ρ) ∈ conv c(L(F)), by the support lemma (outputs confined to S_C, exact-integer) + paired Fourier completeness + an LP mass identity. In this reading the statement is near-definitional — which retroactively explains V48's 5,556/0: those records certify implementations, not a deep conjecture. (ii) The SUBSTANTIVE open form is the compressed-coordinate version (paper C's d=2 correlator vector; V31's actual content). (iii) The previously suggested route — "(D): normalization+marginals+spectral-support cut out aff L, generalizing V43's on-hull lemma" — is **FALSE in general**: pinned counterexamples with dim gaps 14/5/2 (d=4), up to 45 (d=6), 74 (d=8); the census family's rank identity is special. (iv) Correct reduction (proven): quantum ⊆ aff L(F) ⟺ a finite **phase-coherence law** per family — characters in the same ψ-class (equal restriction to ker A ∩ spectral cosets) must carry a single (z,Θ) pair; the LABEL half is proven (Smith-form annihilator + closure), the **Θ-CONGRUENCE half is the new open lemma** (γ_{t'}−γ_t ≡ 2[Σ y_c s_c + Σ h_v (d/g_v) t_v] mod 2d — a cross-context τ-holonomy cancellation of the V45 species), exactly verified on 36 families at d=4,6,8 including every gap case. **[RECORD CORRECTION 2026-07-17: the displayed congruence is shorthand from a session whose notation was never committed — `y_c` and `h_v` have NO operational definition anywhere in this repository (`h_v` occurs only in `papers/obstruction_spectrum_master.tex` with a DIFFERENT meaning, the AvN coboundary), so the formula AS DISPLAYED is not parseable or reproducible from the public artifacts. What IS implemented and verified here is the operational form `[P6] star_check`: every character in a ψ-class carries a single (label, Θ) pair — re-verified on the pinned selftest plus 36 fresh families at d=4,6,8 (30/30 of those in the dimension-gap regime). Cite the `[P6]` property, NOT the displayed formula, until the notation is restored or the formula is withdrawn.] [UPDATE, same day — WELL-POSEDNESS RESTORED: a closed form for γ_t has now been DERIVED from `weyl.py`'s own X,Z definitions and certified against the code's own r-extraction with 0 mismatches over 6,170 characters in 30 fresh families at d=4,6,8, namely γ_t ≡ E(t) − 2(a·λ0_u + b·λ0_v) (mod 2d) with E(t) built from the standard Weyl quadratic form q and λ0 the family base point; the induced γ_{t'}−γ_t is proved gauge-invariant (3,335 cross-context checks, 0 mismatches). This RESTATES the open lemma in fully defined variables — it does NOT recover the lost `y_c`/`h_v` shorthand, which remains undefined. So the Θ-congruence is once again a well-posed open problem, now expressed in (q, λ0). See `arxiv/rigidity/BRANCH_THETA2.md`.]** Also pinned: spec W(v) is a single coset t_v + g_v Z_d (Lemma 0); two V48 comment errata fixed in-file. Default run: `selftest` (expects `[P6] ... PASS`); full modes as CLI. (~1 s selftest.)
- `d4_arithmetic_profile.py` + `ap_species_table.json` (V53): **ARITHMETIC PROFILE OF THE 61 CLASSES COMPLETE (open problem (b), profile part)** — exactly **18 arithmetic species**, completely classified by (integer bound, ||λ||²) where λ is the exact minimal-norm lift into the 150-dim character metric (all rational, denominators ∈ {2,4,6,12} — the (1/12)Z grid); species table with orbit sizes, stabilizers, tight-counts, supports, unit bounds (18 real quadratic fields Q(√m), m ∈ {3,6,7,21,26,31,33,35,37,43,93,159,309,327,345,381,498,534}). Structural facts proven exactly: all 61 classes have tight-rank exactly 32; b′ = cB for all classes; exactly one ghost class (id 59, the τ-twisted ghost triangles); **no class factors through the order-2 shadow — τ-level data is necessary for ALL 61 classes** (sharpens V34). **CORRECTION (pinned): the tier-2 unit bounds "4√2 and 14√2/3" were a NORMALIZATION ARTIFACT** — every tier-2 facet admits an exact {0,±1}-coefficient lift with INTEGER bound ∈ {6,7} (a constant-magnitude 3/(2√2) vector can never attain a NONZERO integer bound on integer vertices — exactness margin asserted); **every facet functional in the census is rational**; Q(√2) is not among the canonical fields (it appears only in the 33-coord metric on classes {48,49,52} — these are bound-7 classes, but NOT the only bound-7 classes: 37 and 53 are too). The V36/V41 "Tsirelson √2 in tier-2" framing should be read at this corrected scope. Also pinned: the stored tier-2 orbit maps onto 14 census classes but is NOT arithmetically closed (four invariant-twin groups straddle the boundary — quantitative confirmation of the V36/V41 completion caveat). General-d conjecture recorded in-script (rational facet data at every even d; real quadratic fields from tight-set Grams; ghost tier persists). Feasibility: full d=6 census defensible on Colab only if |L| ≲ 300 (protocol in-script); d=8 direct conversion out of reach — target ghost tier + transported orbits instead. Expect: `python3 d4_arithmetic_profile.py 1|2|3|4`, each <2 s.
- `pm_local_system.py` (V54): **TWO-LAYER UNIFICATION — PM MILESTONE ACHIEVED (open problem (a))** — the V49/V50 object (rank-1 local system: Gram magnitudes + U(1) holonomy on the non-orthogonality graph) carries the AvN sector too, on the Peres 24-ray configuration of the six PM contexts (9-regular exclusivity graph, H¹ rank 145). (i) **Transversal Parity THEOREM** (proven + verified on all 4096 transversals): every transversal has an odd number of orthogonal shared-observable pairs ⇒ ν(L) = −1, a graph-forced Z₂ invariant = V44's kernel pairing u·s = d/2 in ray language; corollaries α = 5, no independent transversal, CF ≡ 1. (ii) Eigenplane-cycle lemma (proven): every eigenplane 4-cycle carries Bargmann holonomy −1; canonical class h(z_obs) = (−1)⁹ = −1. (iii) **KS-rigidity as the layer switch** (verified numerically, infinitesimal + global 12/12): the PM exclusivity graph pins the ENTIRE local system (flex dimension 0), making all holonomy invariants realization-independent = state-independent; KCBS C₅ has flex dimension 2 = exactly V49's magnitude/holonomy moduli. **Flexes = state-dependent U(1) layer; rigidity + forced torsion = AvN layer.** (iv) Hexagon holonomy law (verified, all 12 K₃,₃ hexagons; proof open, flagged: stabilizer-cocycle expansion should identify −1 with the V44 odd carry): h₆ = −1 exactly on chordless transversal hexagons (192). (v) Interface: ΣP = 6I tight frame ⇒ every state is a top eigenstate of the V49 detector (λ_max = 6 > 5 = α) — state-independence as spectral degeneracy. Caveats pinned: rigidity is d=4-numeric (proof open); no ν = +1 control exists at (2,2) (every closed 3×3 Pauli grid is magic). General dictionary (flex dim = state moduli; rigid + forced torsion ⇔ AvN) stated as conjecture. Expect: `CU UNIFY PROBE PASS` (~3 s).
- **Open (named, carried forward)**: the **Θ-coherence congruence** (V52) remains the sharpest open lemma; a proof attempt was cut short (session limit) and should be resumed — the two-generator transgression closed form (V46-style) is the suggested first step.
- **Audit history (2026-07-11)**: a first review pass over note v3, paper D and the V44–V54 material found no flaws; a second, later pass **did** find real errors in the S4/collapse and observer layers (see the correction ledger below), which are recorded there. Remaining named open items: Θ-coherence congruence (V52), the rigidity dictionary (V54), Conjecture 1 compressed form, general-d census (V53 protocol), paper B generativity, paper A Yu–Oh/gerbe, CMP repair track.

- **CORRECTION LEDGER — measurement-instrument & observer audit (2026-07-11, second external pass):** genuine errors found and fixed; the core Weyl/AvN obstruction-spectrum program (papers A/B, V23–V54 spectrum results) is **not** affected.
  1. **V37 CP range (`lueders_instrument.py`)**: the commutant-covariant depolarizing family's complete-positivity domain was misstated as p∈[0,1]. The exact interval on a rank-r block is **−1/(r²−1) ≤ p ≤ 1** (Choi bound; verified exactly in the new `lueders_cp_interval.py`). The family is **affine**, not the convex [Lüders, depolarize] segment. Lüders (p=1) remains the unique single-Kraus member.
  2. **V37 label**: "machine-exact" → **numerical affine-dimension stress test** (Haar sampling + float lstsq + numerical rank; CP not imposed in that script).
  3. **V40 invalid counterexample (`s4_povm_bayes.py`)**: the pinned qubit example diag(1,c) was invalid — outcome 2 (E₂=diag(0,1−c)) has no eigenvalue 1, so it is not repeatable; the script had checked repeatability only for outcome 1. Replaced by the qutrit example E₁=diag(1,0,c), E₂=diag(0,1,1−c) (every outcome checked); the old example is retained as a pinned negative regression. "repeatable ⇒ projective" is still FALSE, but a witness must be ≥3-dimensional.
  4. **V40 classical range**: the covariant stochastic family is **T_t = t·I + (1−t)·J/m with −1/(m−1) ≤ t ≤ 1**, not [0,1]. Deterministic covariant members: identity (t=1, "Bayes") for all m, plus the **swap (t=−1) at m=2** — so Bayes is the unique deterministic member only for m≥3.
  5. **Paper D "groupoid"**: refinement isometries into larger Hilbert spaces are non-invertible, so the observer structure is a **category (with a switch subgroupoid)**, not a groupoid. Reworded throughout paper D and note v3.
  6. **Paper D stale valuation**: μ_{4d}/even-d half-τ claims removed and synced to V46 (μ_d even, μ_{2d} odd, sharp at d=3).
  7. **Hardware**: the 80σ is **shot-noise-only statistical significance under the implemented witness model**, not a loophole-free contextuality certification; compatibility/nondisturbance controls and systematics are assumptions to be diagnosed (documented in `../hardware/README.md`).
  8. **Interpretive wording**: collapse/Copenhagen statements re-labeled as *characterizations conditional on the stated axioms*, not derivations of outcome occurrence or a solution of the measurement problem.
  Scorecard adjusted off 100 accordingly (S4 and Copenhagen rows); see `plausibility_scorecard.py`.
- Note: `hardware/results/ibm_fez_holonomy_20260709.json` is present (see `../hardware/README.md` for the two distinct ibm_fez runs).
- `mu_d_valuation.py` (V46): **SHARP LOCAL VALUATION + EVEN/ODD INVERSION (THEOREM)** — every commuting Weyl label group admits a trivializer valued in **μ_d for even d** (no τ-steps at all) and **μ_{2d} for odd d, sharp** (the d=3 group ⟨(1,1)⟩ provably requires μ_{2d}; complete search). Proof: parity lemma (c even on commuting pairs, even d) ⇒ c=2c′ symmetric Z_d-cocycle; H²_sym = Ext, additive over cyclic decomposition, detected by cyclic transgressions Σ_j c′(jg,g) = ½a(a−2)q(g) − dK, divisible by a=ord(g) in both parity cases ⇒ class 0. Genuine τ-steps are an ODD-d phenomenon — the inverse of the previous story. Upgrades note v3 Thm 1 to the sharp two-case statement and CLOSES the "μ_{4d} valuation in general" question opened earlier today (answer: μ_d/μ_{2d}, proven). Certificates: parity 120×6 dims; complete μ_d searches (PM, cert4, d=6,8,12); d=3 sharpness; V44-layout regression. Expect: `V46 PASS` (<1 min).
- `tau_gauge_general.py` (V45): **CONSTRUCTIVE q-PARITY GAUGE + τ-GENERAL V44** — (1) q-parity identity: for every closed commuting triple, st(C) ≡ Σ_C q(v) (mod 2), i.e. s_τ = A·q̄ — proof general via Paper B's telescope of c(u,v) = −q(u)−q(v)+2β(u,v)+q(u+v); the GF(2) gauge system is solvable with the explicit solution a = q̄, making V44's integral gauge canonical and gauge-free (1050 triples certified, d=2..12). (2) The full detection equivalence extended to d=6 (m=2): 21 families, 2 unsolvable, spectrum ⊆ {0,3}, double-annihilator, witnesses — all exact. (3) μ_{4d} valuation: every abelian ⟨u,v⟩ at d=6,8,12 admits a μ_{4d} trivializer (complete 2-generator search). PINNED NEGATIVE: the closed-form candidate λ = −2q + (d/2)·(quadratic refinement) FAILS — its residual is not Z₂-bilinear at d=3,4 — so the general μ_{4d} valuation has no one-line proof and remains open (only the valuation; triviality over U(1) is a theorem). Expect: `V45 PASS` (~1 min).

- **CORRECTION + NEW RESULT (2026-07-24, found by a fresh full-program audit)**: **`close_T2_proof.py` did not prove what INDEX cited it for.** Its route to $T_{\mathrm{mix}}\equiv0$ rested on two sufficient conditions, (P1) a per-context vanishing and (P2) a cross-context vanishing. **(P1) is FALSE in bulk** — 424 of 896 lifted contexts at $d=4$ and 344 of 768 at $d=6$ have a nonzero within-context pairing. The script printed `P1: False`, printed no verdict, contained no `assert`, and exited 0, so `run_all.sh` (which gates only on exit code) counted it among "14/14 passed" and this INDEX cited it as "(structural)". **The theorem is nevertheless TRUE** — $T_2=0$ verified directly on all 760 ($d=4$) and 632 ($d=6$) fiber cycles — and the file has been rewritten with a correct proof. *The error was in the proof, not in the result.*
  **The corrected proof.** With $q(x)=\sum_i x_{2i}x_{2i+1}$ the standard GF(2) quadratic refinement ($q(x+y)=q(x)+q(y)+\mathrm{symp}(x,y)$), iterating over a context gives $w(C)=q(S_C)+\sum_{v\in C}q(x_v)$ with $S_C=\sum_{v\in C}x_v$. On a fiber cycle every lifted observable has even multiplicity, so the per-observable term dies and the whole within-context contribution collapses to $\sum_C q(S_C)$. Since $q(S_C)$ depends only on the base context, this is the pairing $\langle n, q(S)\rangle$ where $n(\bar C)$ counts selected lifts of $\bar C$; and $n \bmod 2$ lies in the left-kernel of the BASE incidence matrix. Hence $T_2=0$ iff $q(S)$ is orthogonal to the **realized** sublattice $N=\{n \bmod 2\}$.
  **New structural result (the $d\bmod4$ dichotomy).** For the scaled Mermin-square base $\mathrm{pm\_base}(d)=(d/2)\cdot(\text{qubit labels})$: $N=\{0,(1,1,1,1,1,1)\}$ when $d\equiv0\bmod4$ and $N=\{0\}$ when $d\equiv2\bmod4$ (verified $d=4,\dots,24$; the branches also differ in size, 1536 lifted contexts / 1400 cycles versus 768 / 632). So at $d\equiv2\bmod4$ the vanishing is automatic, while at $d\equiv0\bmod4$ it rests on $\langle\mathbf{1},q(S)\rangle=\mathrm{weight}(q(S))\bmod2$, and $\mathrm{pm\_base}(d)$ has $q(S_{\bar C})=1$ for **exactly two** of its six base contexts at every $d$ tested. The weight-2 parity is load-bearing in that branch. This is the same $d\bmod4$ dichotomy that governs the attained valuation group in V47. Both seeds of the script itself (cert4, PM6) have $N$ trivial; note $\mathrm{pm\_base}(4)$ is a different family from the cert4 seed (1536 lifted contexts against 896).
  **Honest scope.** Orthogonality to the *full* base kernel is sufficient but **not necessary**: random commuting families were found whose $q(S)$ pairs oddly with a base-kernel vector yet still satisfy $T_2=0$ on every fiber cycle, because their $N$ is trivial. No family with $T_2=1$ has been exhibited; whether one exists is **OPEN**. The rewritten script computes $N$ and the pairing explicitly, asserts, and prints PASS/FAIL.
  **Process finding.** `run_all.sh` gates only on exit code, and 6 of its 14 canonical scripts contain no `assert` and print no PASS line (`spectrum_test2`, `criterion`, `close_T2_proof`, `tmix_dindep`, `arf_global`, `thmG_general`). That is precisely why a script printing `False` for its own load-bearing premise sat inside a suite reporting "14/14 passed". The green light is weaker than it looks; adding verdicts/asserts to the remaining five is an open task. **CLOSED 2026-07-24 — see the suite-hardening entry below.**

- **SUITE HARDENING (2026-07-24) — the "process finding" above is now CLOSED.** Every canonical script emits an explicit verdict and asserts its own load-bearing quantities, and `run_all.sh` no longer gates on exit code alone.
  - **Verdicts + assertions added** to the five remaining silent scripts. Each now prints `<name> PASS` and exits nonzero if any check fails. What each asserts: `spectrum_test2` — phase formulas exact on every sampled context; mod-*d* identity violations `0/3000` and odd τ-exponents `0` at *d*=2,4,6,8,12,16; odd-*d* value set exactly `{0}` (25 checks). `criterion` — cert4/cert8 each have ≥1 genuine cycle and a cycle with odd *Q*; **plus a recomputed random-cycle control** (the printed "300/300" was a quoted historical figure the script never recomputed; it now samples 2,500 random *d*=4 families in-run and asserts every genuine cycle has *Q*=0 — 14 cycles found, 0 odd) (6 checks). `tmix_dindep` — signature set and *T* vector byte-identical to *d*=6 for *d*=6,10,14,18,22,26, plus non-vacuity (|T|=128≠0) and coverage of both residues 2 and 6 mod 8 (15 checks). `arf_global` — cert4 fiber pool has 896 contexts and attaining bit 1; controls trivial at base level (5 checks). `thmG_general` — equivariant-hom count = *n* by two independent routes, diagonal image and splittable set both `{0}`, *n*=2..8 (22 checks). `Wformula` also gained a verdict (`Wformula PASS`, 8 checks) so its "violations 0/550" is load-bearing rather than decorative.
  - **`spectrum_test2` section B-strong is an EXPECTED NEGATIVE.** The stronger *mod-2d* refinement (`phexp(C) ?= -2·Σq(u) mod 2d`) is **false**, with violation counts ~860/2000 (*d*=4), 1020/2000 (*d*=8), 1005/2000 (*d*=16). It is exploratory only; Thm J needs the mod-*d* identity of section B. The section is now labelled in-script and its counts are echoed in the verdict block as unasserted, so no future reader mistakes them for a suite failure.
  - **`arf_global`: a wrong comment, not an anomaly.** The inline claim that the random "trivial" bases "should not" attain at fiber level is **FALSE**, and it is **not** a seed artifact. Sweep of 40 seeds × 3 random 6-context *d*=4 bases: **base-level** attaining bit 0 for **120/120** (they really are base-trivial), but **fiber-level** attaining bit 1 for **31/120 (≈26%)**. Reason: `build_fiber_pool` replaces the 6 base contexts by all of their lifts (~768 contexts) whose GF(2) left-kernel is large, so a base-noncontextual family easily acquires an odd-carry cycle after lifting to Z_{2d}. Consequence: these bases are **not** a negative control for fiber attainment, so the script asserts only the base-level triviality (the actual premise) and the cert4 attaining bit; the control fiber bits are printed but deliberately unasserted, with the sweep documented in the file header.
  - **`verify_cert16` gating hole fixed.** It printed `VERDICT: FAIL` on a bad certificate but still exited 0 — the same pathology as `close_T2_proof`. It now asserts `λᵀA ≡ 0 (mod 16)` and `S = 8` and exits nonzero.
  - **`run_all.sh` upgraded.** For each script it now requires (i) exit code 0, (ii) the script's **expected verdict token** present in stdout, and (iii) **no failure token** (`FAILED CHECK`, bare `FAIL`, `VERDICT: FAIL`, `<name> FAIL`, `Traceback`). Any one failing prints a loud diagnostic naming the cause and the suite exits 1. Negative-tested against all three pathologies (silent script, FAIL-but-exit-0, PASS token alongside a `FAILED CHECK` line) — all three are caught. Optional positional args run a subset (`bash run_all.sh spectrum_test2 criterion`) for time-boxed reruns. **Result: 14/14 green under the strict gate**, ~85 s wall total.

- **PACKAGING FIXES (2026-07-24).**
  - **Dead hardcoded paths removed.** `scenario_paired_evidence.py`, `paired_classification_theorem.py` and `d4_arithmetic_profile.py` hardcoded a path from a prior sandbox session (`/sessions/quirky-eloquent-babbage/...`) with a macOS-only fallback, so they could not run on a fresh checkout; `pm_local_system.py` did a `sys.path.append` of the same dead path. All four now derive their paths from `os.path.dirname(os.path.abspath(__file__))` and run from any checkout and any working directory (verified by running each from `/tmp`). `scenario_paired_evidence.py` also now uses a single consistent output directory (`<script dir>/c1_records`, created if absent) instead of two different ones per branch.
  - **V52 default mode.** This INDEX said `paired_classification_theorem.py` has "Default run: `selftest`" while `mode` was a *required* positional argument, so a bare run errored. `mode` is now `nargs='?', default='selftest'` — the documentation is now true (`python3 paired_classification_theorem.py` runs the selftest, ~0.4 s).
  - **Broken `/tmp` chain repaired.** Three scripts consumed scratch files that **no script wrote**, making them unrunnable from a clean checkout. Resolution:
    - `/tmp/v36_facets.npz` — the data (tier-2 facet normals + bounds) *was* computed by `d4_tier2_catalogue.py` (V36) under the local name `facets` but never saved. **The save step is now added there** (`F`, `B`, `idx`), closing the V36 → V41 chain; `d4_tier2_orbit.py` then runs (verified: group order 768, generators biject the 64 vertices, BFS proceeds under its own 700 s budget).
    - `/tmp/v42_proj.npz` — the 64×33 faithful integer projection *was* computed by `d4_exact_dd.py` (V42) `build_Y()` but saved only as `d4_exact_dd_input.npz`. **The save is now also emitted to `/tmp/v42_proj.npz`**, before the optional `cdd` import, so `d4_exact_dd.py` works as a pure producer with no pycddlib installed (it now exits 0 with an install hint instead of an ImportError traceback).
    - `d4_ridge_walk.py` / `d4_heavy_finish.py` additionally need the external **`lrs` binary** (lrslib), which is genuinely not bundled and not pip-installable, plus `/tmp/v43_seed.pkl` (the repo ships the identical object as `d4_group_seed.pkl`, now used as an automatic fallback) and, for `d4_heavy_finish.py`, the BFS state `/tmp/v43_state.pkl` left by `d4_ridge_walk.py`. Both scripts now **guard at the top**: they print a `SKIP … (missing prerequisites)` message naming exactly what is missing and how to produce it, note that their result is already pinned in the repo as `d4_facet_classes.npz` (61 classes / 23,256 facets, verifiable engine-free via `d4_facet_census.py`), and **exit 0**. `d4_tier2_orbit.py` carries the same style of guard for `/tmp/v35_cache.npz` (producer: `d4_odd_sector_facets.py`) and `/tmp/v36_facets.npz` (producer: `d4_tier2_catalogue.py`).

- **NEW THEOREM (2026-07-24): the $d\bmod4$ dichotomy of the realized sublattice, PROVED.** `mod4_dichotomy.py`. The empirical observation recorded above (that $N=\{0,\mathbf{1}\}$ at $d\equiv0\bmod4$ and $N=\{0\}$ at $d\equiv2\bmod4$) is now a theorem for **all even $d$**, not a dimension-by-dimension check. Proof in four steps: **(1)** the base incidence matrix of the Mermin square has GF(2) left-kernel of dimension exactly $1$, spanned by all-ones (each observable lies in exactly two of the six contexts), so $N\subseteq\{0,\mathbf 1\}$; **(2)** $\mathbf 1\in N$ iff some fiber cycle picks an odd number of lifts from every base context, and the minimal such selection is one lift per context, which --- since each base observable lies in exactly two contexts --- is precisely a **globally consistent lift** (a global section of the fiber); **(3)** that is the consistency of an affine GF(2) system in $9\times4=36$ unknowns; **(4)** by bilinearity $\mathrm{symp}(V_i,V_j)=h^2\,\mathrm{symp}(l_i,l_j)$ with $h=d/2$, so the pair-equation right-hand side is $\mathrm{symp}(V_i,V_j)/d=(d/2)s$ where $\mathrm{symp}(l_i,l_j)=2s$, which vanishes mod $2$ **exactly when $d\equiv0\bmod4$**. Hence the entire system (matrix and RHS) is a function of $d\bmod4$ alone --- verified byte-identical within each class up to $d=1002$ --- and two base cases finish it: $d=4$ consistent (rank 20, no inconsistent row), $d=6$ inconsistent (rank 25, 3 inconsistent rows). Confirmed independently for every even $d\le60$ with no exception. **Consequence for the doubling law:** $T_{\mathrm{mix}}=0$ is *automatic* at $d\equiv2\bmod4$ ($N$ trivial), whereas at $d\equiv0\bmod4$ it rests on $\mathrm{weight}(q(S))$ being even --- load-bearing, not automatic. This is the **third independent appearance** of the same $d\bmod4$ dichotomy in the program, alongside V47's attained valuation group and `tmix_dindep`'s $\mathbb F_2$-system $d$-independence.

- **UNIFICATION (2026-07-24): `tmix_dindep`'s $d$-independence and the $N$ dichotomy are ONE fact.** `mod4_dichotomy.py` (function `unification`). `tmix_dindep.py` tests only $d\equiv2\bmod4$ and records the $\mathbb F_2$ system as "$d$-independent" there. That is half of a two-class theorem. Its per-context value is $t=\bigoplus_{a<b}(q_u\wedge1)(M_{ab}\wedge1)$ with $q_u=\mathrm{symp}(u_a,u_b)/d$; for the scaled base $u=h p \bmod d$, bilinearity gives $q_u=h s$ where $\mathrm{symp}(p_a,p_b)=2s$ — **literally the expression driving Step 4 of the $N$ dichotomy**. Hence everything depends on $d$ only through the parity of $h=d/2$: at $d\equiv0\bmod4$ every $q_u$ is even ($0$ of $18$) so $t\equiv0$ **identically, pointwise, with no cycle summation** ($0$ of $1536$ lifted contexts); at $d\equiv2\bmod4$ five of eighteen $q_u$ are odd and $t\neq0$ on $128$ of $768$, so $T_{\mathrm{mix}}$ vanishes only *after* summing over a cycle. Verified $d=4..20$. So both classes are internally constant and behave oppositely, and the two separately-recorded phenomena have a single cause. **Caution recorded:** the V47/V51 naive-section dichotomy is a *different* phenomenon — it concerns **odd** $d$ ($1$ vs $3 \bmod 4$) and comes from the classical Gauss sum $g_d=\sqrt d$ resp. $i\sqrt d$; it is not explained by $q_u=hs$ and should not be conflated with it.
